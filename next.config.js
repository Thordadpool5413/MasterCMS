// Converted from next.config.ts to next.config.js for Next.js compatibility
/** @type {import('next').NextConfig} */
const nextConfig = {
  // add config options here if needed
};

module.exports = nextConfig;
